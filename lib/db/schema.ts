/**
 * Legacy schema export file for backward compatibility
 * All schema definitions have been moved to the /schema folder
 * @deprecated Use imports from "./schema" instead
 */

// Re-export everything from the schema folder for backward compatibility
export * from "./schema";
